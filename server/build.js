//use this file to sync package.json in this directory and the one above it
//keep ALL lines the same except `npm start`, which becomes `"start": "node ./server/bin/www"`

//until this utility is finished, manually sync the two files for now